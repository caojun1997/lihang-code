app:
  name: "pdf-parser"
  host: "0.0.0.0"
  port: 8080
  mode: "debug"

database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "password"
  name: "pdf_parser"
  max_idle_conns: 10
  max_open_conns: 100

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

mineru:
  mode: "flash"
  base_url: "https://api.302.ai"
  api_key: ""
  timeout: 300

upload:
  max_size: 10485760
  storage_path: "./uploads"

result:
  storage_path: "./results"
  cleanup_days: 7

ratelimit:
  requests_per_minute: 60
